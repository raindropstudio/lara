# Lara 저장소 안내

## 먼저 확인할 내용

- `docs/`에는 지속적인 결정, 아키텍처, 제품 맥락을 기록한다.
- `.agents/`에는 벤더 중립적인 작업 자료만 둔다.
- `lara-backend`와 `lara-frontend`는 참고용 레거시 저장소다.
- 새 코드는 이 워크스페이스에 작성한다. 레거시 Git 이력은 가져오지 않으며 이 저장소의 작업 커밋은 보존한다.

## 작업과 커밋

- 시작할 때 Git 상태를 확인하고 이전 AI 작업이 남아 있으면 변경 범위를 확인해 기준점 커밋을 만든다. 사용자 변경은 임의로 포함하지 않는다.
- 검증 가능한 기능이나 수정 단위가 완성될 때 관련 검증 후 커밋한다. 요청받지 않은 push, 이력 재작성은 하지 않는다.
- 화면 변경은 데스크톱과 모바일 크기에서 브라우저로 렌더링과 주요 동작을 확인한다.

## 표준 명령

- 설치: `corepack enable && pnpm install`
- API와 웹 개발: `pnpm dev`
- 단일 앱 개발: `pnpm dev:api`, `pnpm dev:web`, `pnpm dev:collector`
- 전체 검증: `pnpm check`
- 변경 영향 검증: `pnpm nx affected -t lint,typecheck,test,build`

## 경계

- 앱은 패키지에 의존할 수 있지만 패키지는 앱에 의존하지 않는다.
- 앱 사이의 런타임 import를 금지한다.
- 웹은 `@lara/api/eden`에서 Elysia `App` 타입만 import할 수 있다. 이 경계에 런타임 값이나 내부 구현 타입을 노출하지 않는다.
- HTTP 계약을 미리 별도 패키지로 복제하지 않는다. 큐 작업과 저장 형식처럼 HTTP 밖에서도 쓰는 안정된 계약만 공유 패키지로 둔다.
- Nexon 원본 형식을 웹이나 공개 API 계약에 노출하지 않는다.
- 경계가 안정됐거나 두 번째 사용처가 생긴 코드만 `packages/`로 옮긴다.

## Nexon 데이터

- 기본 테스트에서 실제 Nexon API를 호출하지 않는다.
- 응답 기록은 저장소 recorder, 허용된 캐릭터 목록, `fixtures/nexon/`의 manifest 형식을 사용한다.
- API 키, 인증 헤더, 쿠키, 자격 증명을 식별할 수 있는 헤더를 커밋하지 않는다.
- 기록한 응답 본문은 가공하거나 포맷하지 않는다.
- 한 endpoint의 파싱 실패가 다른 endpoint 데이터를 무효화하면 안 된다.

## 문서와 주석

- 기본 문서와 코드 주석은 특별한 이유가 없으면 간결한 한글로 작성한다.
- Markdown 등 산문 문서는 문단과 목록 항목 단위로 한 줄을 유지하며, 열 너비에 맞추기 위한 강제 개행을 하지 않는다.
- 명령어, 식별자, 경로, 외부 API 필드명은 원문을 유지한다.
- 코드만으로 명확한 내용은 주석으로 반복하지 않는다.

## 완료 기준

- `pnpm check`를 실행하고 생략한 검증이 있으면 알린다.
- 부분 성공과 오류를 포함해 변경 동작을 검증하는 테스트를 추가한다.
- 승인된 결정의 의미는 수정하지 않고 새 결정으로 대체한다.
- 작업 로그와 에이전트 대화는 Git에 저장하지 않는다.
