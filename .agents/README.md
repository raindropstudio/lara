# 에이전트 자료

이 폴더에는 벤더 중립적인 반복 작업 자료를 둔다. 지속적인 프로젝트 맥락은
`docs/`, 실행 로직은 저장소 도구에 둔다.

Nexon fixture 기록이나 원본 스키마 변경 대응처럼 반복할 작업에만 skill을 만든다.
skill은 로직을 설명문으로 복제하지 않고 재현 가능한 저장소 명령을 호출한다.

임시 작업 기록과 대화는 Git에서 제외된 `.agents/runs/`에 둔다.
