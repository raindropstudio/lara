{
  "name": "@lara/collector",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint .",
    "start": "node dist/main.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lara/character-pipeline": "workspace:*",
    "@lara/data-store": "workspace:*",
    "@lara/jobs": "workspace:*",
    "@lara/nexon-client": "workspace:*",
    "bullmq": "catalog:",
    "redis": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "nx": {
    "tags": [
      "scope:collector",
      "type:app"
    ]
  }
}
